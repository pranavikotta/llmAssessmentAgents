colang_version: "1.0"

models:
  - type: main
    engine: openai
    model: grok-4-1-fast-non-reasoning
    parameters:
      api_key: "$env(GROK_API_KEY)"
      base_url: "https://api.x.ai/v1"

rails:
  input:
    flows:
      - block adversarial input
      - block credential request
  output:
    flows: []